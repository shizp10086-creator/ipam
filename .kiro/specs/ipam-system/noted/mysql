# 1. 停止并删除旧容器
docker stop mysql8.0
docker rm mysql8.0
# 2. 重新启动带数据卷挂载的容器
docker run -d `
  --name mysql8.0 `
  -p 3306:3306 `
  -e MYSQL_ROOT_PASSWORD=putailai123 `
  -v D:/mysql-data:/var/lib/mysql `
  mysql:8.0

  Windows 下 MySQL 容器常用管理命令清单
一、容器基础管理（核心）
表格
操作目的	PowerShell/CMD 命令
启动 MySQL 容器（基础版）	docker run -d --name mysql8.0 -p 3306:3306 -e MYSQL_ROOT_PASSWORD=123456 mysql:8.0
启动 MySQL（带持久化 + 中文 + 时区）	docker run -d --name mysql8.0 -p 3306:3306 -e MYSQL_ROOT_PASSWORD=123456 -e TZ=Asia/Shanghai -e MYSQL_INITDB_ARGS="--character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci" -v D:/mysql-data:/var/lib/mysql mysql:8.0
启动已停止的容器	docker start mysql8.0
停止运行中的容器	docker stop mysql8.0
重启容器	docker restart mysql8.0
查看运行中的容器	docker ps
查看所有容器（含停止）	docker ps -a
查看容器日志（排错用）	docker logs mysql8.0（实时日志：docker logs -f mysql8.0）
删除停止的容器	docker rm mysql8.0
强制删除运行中的容器	docker rm -f mysql8.0
二、进入容器 / 连接 MySQL
表格
操作目的	命令
进入 MySQL 容器命令行	docker exec -it mysql8.0 bash
容器内连接 MySQL（root）	先进入容器，再执行：mysql -uroot -p（回车后输入密码 123456）
直接连接容器内 MySQL	docker exec -it mysql8.0 mysql -uroot -p（无需先进入容器）
修改 root 密码（容器内）	连接 MySQL 后执行：ALTER USER 'root'@'%' IDENTIFIED BY '新密码';
授权 root 远程访问（容器内）	连接 MySQL 后执行：GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' WITH GRANT OPTION; FLUSH PRIVILEGES;
三、数据备份 / 恢复（重要）
表格
操作目的	命令
备份 MySQL 数据到本地	docker exec mysql8.0 mysqldump -uroot -p123456 --all-databases > D:/mysql-backup/all-db-$(date -Format "yyyyMMdd").sql
从本地恢复数据到 MySQL	docker exec -i mysql8.0 mysql -uroot -p123456 < D:/mysql-backup/备份文件.sql
四、镜像管理
表格
操作目的	命令
查看本地 MySQL 镜像	docker images mysql
删除 MySQL 镜像	docker rmi mysql:8.0（需先删除依赖容器）
拉取指定版本 MySQL 镜像	docker pull mysql:8.0.45（指定具体版本，避免自动更新）
关键说明
所有命令中mysql8.0是容器名，123456是 root 密码，可根据你的实际配置修改；
备份命令中$(date -Format "yyyyMMdd")是 PowerShell 的日期格式，CMD 中需替换为%date:~0,4%%date:~5,2%%date:~8,2%；
挂载目录D:/mysql-data需确保本地已创建该文件夹，否则 Docker 会自动创建（建议手动创建，避免权限问题）。
总结
日常管理优先用start/stop/restart操作容器，避免频繁删除重建导致数据丢失；
数据备份是核心操作，建议定期执行，防止容器异常导致数据丢失；
远程连接失败时，先检查容器端口映射（-p 3306:3306）和 root 远程访问权限。
如果有某个命令执行报错，或者想补充其他场景的命令（比如批量操作、自动启动），可以告诉我。

根据 `docker-compose.yml` 配置，Docker Compose 模式下的数据库密码信息如下：

## 🔐 MySQL 数据库密码信息

### 应用连接账户（后端使用）
- **用户名**: `ipam`
- **密码**: `ipam_password`
- **数据库**: `ipam_db`
- **主机**: `mysql` (容器内) 或 `localhost` (宿主机)
- **端口**: `3306`

### Root 管理员账户
- **用户名**: `root`
- **密码**: `root_password`

## 📝 连接方式

### 从宿主机连接（Windows）
```powershell
# 使用应用账户
docker exec -it ipam-mysql mysql -uipam -pipam_password ipam_db

# 使用 root 账户
docker exec -it ipam-mysql mysql -uroot -proot_password
```

### 从容器内连接
后端服务通过环境变量自动连接：
```
MYSQL_HOST=mysql
MYSQL_USER=ipam
MYSQL_PASSWORD=ipam_password
MYSQL_DATABASE=ipam_db
```

## ⚠️ 安全提示

这些是默认密码，建议在生产环境中修改：

1. 编辑 `docker-compose.yml`
2. 修改以下配置：
   ```yaml
   environment:
     MYSQL_ROOT_PASSWORD: 你的新root密码
     MYSQL_PASSWORD: 你的新应用密码
   ```
3. 同步更新 `backend/.env` 中的密码
4. 重新部署：`docker compose down && docker compose up -d`